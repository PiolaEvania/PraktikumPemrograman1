#include <stdio.h>

int main()
{
    char nm[50], nim[15], kp[2], ttl[50], al[100], hb[100], nhp[15];

    printf("Nama                 :");
    gets(nm);
    printf("NIM                  :");
    gets(nim);
    printf("Kelas Paralel        :");
    gets(kp);
    printf("Tempat/Tanggal Lahir :");
    gets(ttl);
    printf("Alamat               :");
    gets(al);
    printf("Hobby                :");
    gets(hb);
    printf("No. HP               :");
    gets(nhp);
}
